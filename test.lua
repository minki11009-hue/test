graph TD
    U[사용자] -->|요청| C[클라이언트 (웹/앱)]
    C -->|API 요청| S[서버 (백엔드)]
    S -->|CRUD 작업| D[(데이터베이스)]
    S -->|파일 업로드/다운로드| ST[(스토리지 - 이미지/동영상)]
    S -->|외부 호출| EX[(외부 서비스 - 결제, 인증, 지도 등)]
    S -->|이벤트/로그| AN[(로깅 & 분석 시스템)]
    S -->|관리자 API| A[관리자 페이지]
    A -->|관리 & 통계| D
    S -->|응답| C
    C -->|결과 화면| U
    subgraph Infra[인프라 계층]
        CDN[CDN / 캐시 서버]
        LB[로드밸런서]
        SEC[보안 / 인증]
        DEP[배포 시스템(CI/CD)]
    end
    CDN --> C
    C -->|정적 파일| CDN
    S --> LB
    LB --> S
    SEC --> S
    DEP --> S
